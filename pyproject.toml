[project]
name = "benchmark-citations"
version = "0.1.0"
description = "Tracks benchmark citations across the LLM ecosystem (benchmark is the topline entity)."
requires-python = ">=3.12"
dependencies = ["pyyaml>=6.0"]

[tool.uv]
dev-dependencies = ["ruff>=0.6", "pytest>=8"]
